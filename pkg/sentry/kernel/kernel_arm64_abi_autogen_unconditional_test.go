// Automatically generated marshal tests. See tools/go_marshal.

package kernel

func Example() {
    // This example is intentionally empty, and ensures this package contains at
    // least one testable entity. go-marshal is forced to emit a test package if the
    // input package is marked marshallable, but emitting no testable entities 
    // results in a build failure.
}
